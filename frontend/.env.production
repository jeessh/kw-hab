# Same-origin prod: the frontend calls the backend through the /api rewrite.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_SITE_URL = https://kw-hab.vercel.app
INTERNAL_API_URL     = https://kw-hab.vercel.app/api